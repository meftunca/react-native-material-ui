import {action} from '@storybook/addon-actions';
import {Story, Meta, Props, Preview} from '@storybook/addon-docs/blocks';
import {withKnobs, select, boolean} from '@storybook/addon-knobs';
import {Menu, Icon,FormControl, Palette,Button, Text} from '@devloops/react-native-variant';
import {FormControlProps} from './FormControlProps.js';
import {View} from "react-native"

<Meta title="Components|Form/Control" />

## Usage

<Story name="Usage">
<View style={{padding: 8,flexDirection:"column",}}>
    <h4>Disabled Example</h4>    
    <View style={{flexDirection: 'row'}}>
        <FormControl size={24} disabled />
        <FormControl type="radio" size={24} disabled />
    </View>
    <h4>Basic Example</h4>    
   <View style={{flexDirection: 'row'}}>
        <FormControl size={24} />
        <FormControl type="radio" size={24} />
    </View>
     <h4>Color&Size Example</h4>    
   <View style={{flexDirection: 'row'}}>
        <FormControl size={20} color={Palette.red500} />
        <FormControl type="radio" size={20} color={Palette.red500} />
    </View>
    <View style={{flexDirection: 'row'}}>
        <FormControl size={18} color={Palette.green500} />
        <FormControl type="radio" size={18} color={Palette.green500} />
    </View>
    <View style={{flexDirection: 'row'}}>
        <FormControl size={16} color={Palette.orange500} />
        <FormControl type="radio" size={16} color={Palette.orange500} />
    </View>
</View>
</Story>    

<br />

```js dark
<View style={{padding: 8,flexDirection:"column",}}>
    <View style={{flexDirection: 'row'}}>
        <FormControl size={24} disabled />
        <FormControl type="radio" size={24} disabled />
    </View>
    <View style={{flexDirection: 'row'}}>
        <FormControl size={24} />
        <FormControl type="radio" size={24} />
    </View>
    <View style={{flexDirection: 'row'}}>
        <FormControl size={20} color={Palette.red500} />
        <FormControl type="radio" size={20} color={Palette.red500} />
    </View>
    <View style={{flexDirection: 'row'}}>
        <FormControl size={18} color={Palette.green500} />
        <FormControl type="radio" size={18} color={Palette.green500} />
    </View>
    <View style={{flexDirection: 'row'}}>
        <FormControl size={16} color={Palette.orange500} />
        <FormControl type="radio" size={16} color={Palette.orange500} />
    </View>
</View>
```

<Props of={FormControlProps} />