import {action} from '@storybook/addon-actions';
import {Story, Meta, Props, Preview} from '@storybook/addon-docs/blocks';
import {withKnobs, select, boolean} from '@storybook/addon-knobs';
import {Icon} from '@devloops/react-native-variant';
import IconProps from './Icon.js';

<Meta title="Core|Icon" component={IconProps} />

# Icon

<br />
<hr />
<br />

### Cross Platform Support

1. [x] Mobile
2. [x] Web (It's now can't support for react-native-vector-icons, maybe later support can be given)

<br />
<hr />
<br />

### Basic Usage

<Icon name="add" style={{marginRight: 16}} size={32} color="green" />
<Icon name="face" style={{marginRight: 16}} size={40} color="red" />
<Icon name="map" style={{marginRight: 16, fontSize: 48,color:"blue"}} />

```js dark
<Icon name="add" size={32} color="purple" />
<Icon name="face" size={40} color="red" />
<Icon name="map" style={{fontSize: 48,color:"blue"}} />
```

<Props of={IconProps} />
